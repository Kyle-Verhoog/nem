language: python
python:
- '3.7'
sudo: false
env:
- export CI=travis
cache:
  directories:
  - $HOME/.cache/pip
install:
  - pip install git+https://github.com/kyle-verhoog/nem.git
  - pip install -r requirements.txt
script:
- python -m unittest discover -v tests/
